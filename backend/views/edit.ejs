<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <main>
        <div>
            <form action="/posts/<%= editPost._id %>?_method=PUT" method="post">
                <fieldset>
                    <legend>Edit a <%= editPost.title %>
                    </legend>
                    <label>
                        title:<input type="text" name="title" value="<%= editPost.title %>" placeholder="enter fruit title" />
                    </label>
                    <label>
                        post body:<input type="text" name="body" value="<%= editPost.body %>" placeholder="enter post body" />
                    </label>
                    
                </fieldset>
                <input type="submit" value="Update <%= editPost.title %>" />
            </form>
        </div>
    </main>
</body>
</html>
